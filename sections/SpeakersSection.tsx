import { Reveal } from "@/components/Reveal";
import { eventConfig } from "@/config/event";
import { MotionHeading } from "@/components/motion/MotionHeading";
import { SectionTransition } from "@/components/motion/SectionTransition";

export function SpeakersSection(){return <section id="intervenants" className="cinematic-section speakers-rebuilt speakers-premium"><div className="wrap"><Reveal variant="lateralLeft"><p className="eyebrow">Les intervenants</p><MotionHeading>Des profils qui construisent réellement.</MotionHeading><p className="section-copy">Des entrepreneurs viennent partager leurs décisions, leurs erreurs et les stratégies qui leur permettent de faire progresser leurs projets. Des retours d'expérience concrets, dans un cadre propice aux vraies conversations.</p></Reveal><div className="speaker-premium-grid">{eventConfig.speakers.map((speaker,index)=>{const mystery=speaker.status==="EN SÉLECTION";const initials=mystery?"DB":speaker.name.slice(0,2).toUpperCase();return <Reveal variant={mystery?"quiet":"stately"} className={`speaker-premium ${mystery?"speaker-mystery":""}`} key={speaker.id} delay={index*.07}><div className="speaker-premium-visual" role="img" aria-label={mystery?`Intervenant ${speaker.id} en sélection`:`Portrait éditorial temporaire de ${speaker.name}`}><div className="portrait-architecture" aria-hidden><span>{initials}</span><i/></div></div><div className="speaker-premium-copy"><small>{speaker.id} — {speaker.expertise}</small><h3>{speaker.name}</h3><p>{speaker.role}</p>{speaker.result&&<><strong>{speaker.result}</strong><em>Revenu SaaS annoncé par l'intervenant</em></>}<b>{speaker.status}</b></div></Reveal>})}</div></div><SectionTransition type="dark-veil"/></section>}
